<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="@color/white">

    <!-- Yoga Illustration inside CardView for Rounded Corners -->
    <androidx.cardview.widget.CardView
        android:layout_width="200dp"
        android:layout_height="250dp"
        android:layout_marginTop="50dp"
        app:cardCornerRadius="50dp"
        app:cardElevation="5dp"
        android:layout_gravity="center">

        <ImageView
            android:id="@+id/yogaImage"
            android:layout_width="200dp"
            android:layout_height="250dp"
            android:layout_marginTop="50dp"
            android:background="@drawable/rounded_image"
            android:scaleType="fitCenter"
            android:src="@drawable/img_18" />

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Feel Stronger"
        android:textSize="35sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginTop="20dp"/>

    <TextView
        android:id="@+id/subtitleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Transform your mind, body, and soul with the power of yoga—breathe deeply, move freely, and live mindfully every day."
        android:textSize="20sp"
        android:textColor="@color/black"
        android:gravity="center"
        android:layout_marginTop="20dp"/>

    <Button
        android:id="@+id/getStartedButton"
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:layout_marginTop="50dp"
        android:layout_marginBottom="20dp"
        android:backgroundTint="@color/green"
        android:text="Get Started"
        android:textColor="@color/white"
        android:textSize="20sp" />

</LinearLayout>
